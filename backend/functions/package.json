{
  "name": "erp-gep-functions",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=20.18.0",
    "npm": ">=10.8.0"
  },
  "scripts": {
    "dev": "echo \"Ejecuta netlify dev desde la raíz o usa 'netlify functions:serve'\"",
    "build": "echo \"Netlify compila automáticamente las funciones (esbuild). No se requiere build manual.\"",
    "typecheck": "tsc -p ../tsconfig.json --noEmit"
  },
  "dependencies": {
    "@netlify/functions": "^4.2.7"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "typescript": "^5.9.3"
  }
}
