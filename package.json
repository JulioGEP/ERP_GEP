{
  "name": "erp_gep_monorepo",
  "private": true,
  "version": "1.0.9",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.8.0"
  },
  "scripts": {
    "clean": "rm -rf node_modules .prisma",
    "build": "npm run build:frontend",
    "prebuild:frontend": "if [ ! -x frontend/node_modules/.bin/vite ]; then npm install --prefer-offline --include=dev --include-workspace-root --workspaces; fi",
    "build:frontend": "npm run build --workspace frontend",
    "netlify:build": "npm run -w backend generate && npm run -w backend prisma:prune && npm run build",
    "dev": "echo \"Dev local: usa 'npx netlify dev -p 8888' si tienes Netlify CLI disponible\"",
    "prisma:generate": "npm run -w backend generate",
    "typecheck:functions": "npm run -w backend typecheck"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.901.0",
    "@aws-sdk/s3-request-presigner": "^3.901.0",
    "@prisma/client": "^5.22.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@netlify/functions": "^4.2.7",
    "@types/node": "^20.19.19",
    "dotenv-cli": "^10.0.0",
    "netlify-cli": "^17.38.1",
    "pino-pretty": "^13.1.1",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  },
  "prisma": {
    "schema": "backend/prisma/schema.prisma"
  }
}
