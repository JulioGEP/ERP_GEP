import { SessionModal, type SessionModalProps } from '../SessionModal';

export type MaterialsSessionModalProps = SessionModalProps;

export function MaterialsSessionModal(props: MaterialsSessionModalProps) {
  return <SessionModal {...props} />;
}
