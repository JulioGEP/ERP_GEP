import { SessionModal, type SessionModalProps } from '../SessionModal';

export type GepServicesSessionModalProps = SessionModalProps;

export function GepServicesSessionModal(props: GepServicesSessionModalProps) {
  return <SessionModal {...props} />;
}
